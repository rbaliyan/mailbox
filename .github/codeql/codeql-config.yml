name: "CodeQL Config"

query-filters:
  # store/mongo/query.go uses buildFilter which validates all keys via MessageFieldKey
  # whitelist and values are typed Go objects, not raw strings. The nosql-injection
  # alerts on Find/Count/Search are false positives.
  - exclude:
      id: go/nosql-injection
