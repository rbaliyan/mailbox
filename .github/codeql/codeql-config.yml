name: "CodeQL Config"

# store/mongo/query.go uses buildFilter which validates all keys via MessageFieldKey
# and values are typed Go objects, not raw strings. Exclude from sink reporting to
# suppress false-positive nosql-injection alerts on Find/Count/Search.
paths-ignore:
  - store/mongo/query.go
